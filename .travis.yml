sudo: true
language: bash
python:
  - 3.7-dev
services:
  - docker
addons:
  apt:
    packages:
      - docker-ce

deploy:
  - provider: script
    script: bash ./deploy.sh
    on:
      branch: master
