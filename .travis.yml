sudo: true
language: python
python:
  - 3.7.2
services:
  - docker
addons:
  apt:
    packages:
      - docker-ce

deploy:
  - provider: script
    script: bash ./deploy.sh
    on:
      branch: master
